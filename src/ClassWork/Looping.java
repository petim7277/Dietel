package ClassWork;
//import java.util.Scanner;
public class Looping {
    public static void main(String[] args) {



/*write a code that grades students based on their scores the range will be as follows.
scores given are 90 to 100,80 to 89, 70 to 79 , 60 to 69 and less than 60.
if student score falls between the range of 90 to 100 output grade  as A
if student score falls between the range of 80 to 89 output grade as B
if student score falls between the range of 70  to 79 output grade as C
if student score falls between the range of 60 to 69 output grade as D
if student score falls between the range of 60 and below output grade as F
 */
        //Scanner input=new Scanner(System.in);
 /* System.out.print("Enter your Score");
    int userInput =input.nextInt();
    if(userInput>89&&<101);*/
//int count= 1;

        // while (count<=1000){
        //  for (int count = 1; count <= 1000 ;count =count+1) {
        //    System.out.println(count);

        //  }

        //for (int i = 5; i > 0 ; i--) {
        //  System.out.println("man");

        // }

        // for (int count = 1;  count<5 ; count++) {
        //   System.out.println("precious");

        //      }


      //  for (int count = 0;  count<1001 ; count++) {
        //    System.out.println(count);
       // }

       /* for (int count = 0; count <1 ; count++) {
            System.out.println("*");
        }
        for(int count = 5; count <5 ; count++) {
            System.out.print("*");
        }
        for (int count = 0; count <3 ; count++) {
            System.out.print("*");
        }
        for (int count = 0; count <4 ; count++) {
            System.out.print("*");
        }*/
       // int number= 234;
        //while (number>30){
         //   System.out.printf("infinite loop");
          //  number=+1;
        //}














    }
}