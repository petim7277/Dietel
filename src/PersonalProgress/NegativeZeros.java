/*package PersonalProgress;

import Assignments.NegativeNumbers;

import java.util.Scanner;

public class NegativeZeros  public String numberDeterminer(int numZero, int positiveNumber, int negativeNumber){

    return String.format("""
                Number of Zero  %d
                Number of Negative number %d
                Number of Positive number %d
                """,numZero,positiveNumber,negativeNumber);

}
        public static void main(String[] args) {
            int negatifNumber = 0;
            int positifNumber = 0;
            int numZero = 0;
            Scanner input = new Scanner(System.in);
            Scanner input2 = new Scanner(System.in);
            NegativeNumbers negativeNumbers = new NegativeNumbers();
            while (true){
                System.out.println("Enter your number:  ");
                int number = input.nextInt();
                if (number < 0 ) negatifNumber ++;
                if (number > 0 ) positifNumber ++;
                if (number == 0 ) numZero ++;
                System.out.println("enter -1 to quit \n press any thing to continue");
                if (number < 0) {
                }
                if (option.equals("#")){
                    negativeNumbers.numberDeterminer(numZero,positifNumber,negatifNumber);
                    break;
                }
            }{
}
*/